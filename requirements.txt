# Core dependencies for Schoology API Scraper
python-dotenv>=1.0.0
requests>=2.31.0
requests-oauthlib>=1.3.1
pydantic>=2.0.0

# AI grade summaries (optional at runtime, provider loads only if gemini_key is set)
google-genai>=1.0.0

# Development and testing
pytest>=7.4.0
